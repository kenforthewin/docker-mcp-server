version: '3.8'

services:
  mcp-container:
    build:
      context: .
      dockerfile: Dockerfile.debian
    container_name: mcp-container
    ports:
      - "3000:3000"
    volumes:
      - ./tmp:/app/workspace
    environment:
      - NODE_ENV=production
    restart: unless-stopped
